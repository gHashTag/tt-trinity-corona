# Loop 58 Report -- 2026-06-02

## Summary

**Deep audit: 0 CRITICAL, 0 HIGH, 1 MEDIUM (documented), 7 LOW. 8th consecutive precheck PASS (GDS #26784554391).** New audit areas: cocotb 2.x compatibility, formal depth adequacy, GLS/cocotb parity, gen_rom.py consistency, D2D stubs, Makefile lint, fmt_id=0 edge case, SDC completeness. Found and documented `total_bits` 8-bit overflow for fp256/GF256 in gen_rom.py. Added fmt_id=0 to NOT_IMPL test coverage. All CI jobs pass with 47 tests (including Loop 57's BCD exhaustive and TF32 random).

## Weakness Audit: 8 New Areas

### Confirmed Correct (7/8 areas)

| Area | Finding |
|------|---------|
| Cocotb 2.0.1 compat | Clean. All API calls are 2.x-native (`start_soon`, `.value`, `@cocotb.test`). No deprecated patterns |
| Formal depth | Depth 30 is 2.5x worst-case 12-cycle transaction. k-induction proofs close with 7 auxiliary invariants |
| GLS vs cocotb parity | GLS: 76 tests (hardcoded golden values, synthesis-level). Cocotb: 47 tests (exhaustive sweeps, RTL-level). GLS test 76 (mid-transaction reset) unique to GLS, covered by formal P5 |
| D2D routing stubs | FLAG_D2D is ROM metadata only. No dead code or unused wires in RTL |
| Makefile lint | `make lint` = `iverilog -t null -Wall -g2012`, identical to CI. Verilator lint also runs separately |
| fmt_id=0 behavior | Correctly produces NOT_IMPL via `default` case. Now tested explicitly |
| SDC completeness | Adequate for TT flow. Missing constraints (clock_uncertainty, driving_cell) handled by TT infrastructure |

### MEDIUM (1, documented)

| # | Finding | Status |
|---|---------|--------|
| M1 | `total_bits` 8-bit field overflows for fp256/GF256 (256 & 0xFF = 0) | **DOCUMENTED** — warning added to gen_rom.py. No silicon impact (no decoder reads total_bits). Convention: 0 means 256 |

### LOW (7, informational)

| # | Finding | Status |
|---|---------|--------|
| L1 | GLS test 76 (mid-transaction reset) not in cocotb | Covered by formal P5 (state transition validity) |
| L2 | gen_rom.py `pack_record` truncates silently | **FIXED** — warning now emitted to stderr |
| L3 | No `set_clock_uncertainty` in SDC | Negligible at 25 MHz on GF180MCU |
| L4 | `info.yaml` discord field still empty | User decision (carried from Loop 55) |
| L5 | Viewer job fails in GDS workflow | Cosmetic, known issue across all runs |
| L6 | VCD files in working tree (gitignored) | Non-issue |
| L7 | 57 report files in docs/ | Development audit trail |

## Implementation

### gen_rom.py overflow warning (M1 fix)

Added overflow detection to `tools/gen_rom.py:pack_record()`:
- Emits `WARNING: fmt_id=N total_bits=256 truncated to 0 (8-bit field overflow)` to stderr
- Fires for fmt_id=4 (fp256) and fmt_id=29 (GF256)
- ROM output unchanged (already stored 0; this documents the convention)

### fmt_id=0 NOT_IMPL coverage

Added `fmt_id=0` to `test/test_stress.py:test_not_impl_response`:
- Previous list: `[5, 6, 7, 15, 20, 50, 60]`
- New list: `[0, 5, 6, 7, 15, 20, 50, 60]`
- Confirms the reset-default `fmt_id_r=0` correctly produces NOT_IMPL sentinel

### Verification: gen_rom.py determinism

Regenerated `format_rom.v` from `gen_rom.py` — output is byte-identical to committed ROM. Packing is deterministic and round-trip verified (format_index_id self-check for all 80 records).

## Scientific Research

### Ongoing from Loop 57

Key findings from Loop 57 research remain current:
- **AetherFloat** (arXiv:2603.08741): base-4 FP, block-scale-free AF8/AF16
- **FLoPS** (arXiv:2602.15965): P3109 formal verification in Lean
- **ARITH 2026** (June 28, Fulda): P3109 paper, TensorGauge, Muller keynote
- **ScaleSearch** (arXiv:2605.12464): optimal BFP scales
- **MX+** (arXiv:2510.14557 / MICRO '25): outlier-aware MX extension
- **Essenceia**: FP systolic array tapeout on TTIHP26a

### Shuttle Status

- TTGF26a: 20 days to deadline (June 22)
- TTSKY26c: new shuttle, launched May 26, closes September 7
- 8 consecutive precheck passes confirmed

## GDS Status

```
Precheck passes: 8 CONSECUTIVE
GDS #26784554391:  PASS (Loop 57 commit — GDS ✅, GL tests ✅, precheck ✅)
GDS #26783912361:  PASS (Loop 56 report)
GDS #26783122983:  PASS (Loop 55 final report)
GDS #26782301814:  PASS (test fix)
GDS #26781517933:  PASS (doc updates)
GDS #26781259723:  PASS (Loop 54 report)
GDS #26780418485:  PASS (MARGIN_MULT fix — first pass)
All with: BOTTOM_MARGIN_MULT=1, TOP_MARGIN_MULT=1
```

## Verification Status — ALL GREEN

```
Verilator lint:        PASS (0 warnings)
Yosys synthesis:       PASS (2,308 cells)
Formal (sby):          19 configs, 57 tasks (bmc+prove+cover)
Cocotb:                47 tests (fmt_id=0 added to NOT_IMPL coverage)
GL tests:              PASS (47/47)
GLS smoke:             PASS (76 golden vectors)
GitHub CI:             PASS (all green on commit 2308b24)
GDS + Precheck:        PASS (8 consecutive)
Submission status:     READY
```

## Audit Coverage Summary (Loops 54-58)

All 16 audit areas now investigated:

| Loop | Areas | Key Findings |
|------|-------|-------------|
| 54 | Precheck root cause | MARGIN_MULT fix (CRITICAL, fixed) |
| 55 | Shift register, mux, docs | byte_count test, LibreLane rebrand (4 fixed) |
| 56 | Multi-byte alignment, ROM golden, timing, pins, reset, unused outputs | All 8 clean |
| 57 | FSM transitions, ROM completeness, CDC, synthesis, test gaps, CI, tiles | TF32+BCD tests (2 fixed) |
| 58 | Cocotb compat, formal depth, GLS parity, gen_rom, D2D, Makefile, fmt_id=0, SDC | total_bits overflow (documented) |

**Total: 0 CRITICAL, 1 HIGH (discord empty, user decision), 1 MEDIUM (documented), 14 LOW remaining.**

## Metrics

- New audit areas investigated: 8 (all clean or documented)
- Issues found: 0 critical, 0 high, 1 medium (documented), 7 low
- gen_rom.py: verified deterministic, overflow warning added
- fmt_id=0: confirmed NOT_IMPL, added to test
- Precheck consecutive passes: 8
- CI status: all green (47 cocotb + 76 GLS + 57 formal)
- Days to deadline: 20

---

## Three Collaboration Options for Next Loop

### Option A: Final Submission — Fill Discord + Freeze

All 16 audit areas investigated across 5 loops. 8 consecutive precheck passes. 0 blocking issues. Fill the discord field in info.yaml, make a final commit, submit to TTGF26a, and freeze the repo. The 20-day buffer is ample. Focus shifts to post-silicon: RP2040 firmware for test board, tt-commander Python framework, ARITH 2026 format coverage summary.

### Option B: Add Mid-Transaction Reset Test + GLS/Cocotb Parity

The one test gap found: GLS test 76 (mid-transaction reset recovery) has no cocotb equivalent. Add `test_mid_transaction_reset` to test_stress.py — assert reset during CMD2/DATA states and verify FSM recovers correctly. This closes the GLS/cocotb parity gap and gives 48 cocotb tests. Low risk, high confidence boost.

### Option C: AetherFloat ROM Entry + Post-Silicon Test Framework

Add AetherFloat AF8 (arXiv:2603.08741) as ROM entry 80 — the newest base-4 format that eliminates block-scaling hardware. Build the tt-commander Python framework skeleton for post-silicon validation via RP2040 serial interface. This combines catalog expansion with practical test infrastructure for when the chip returns in October 2026.
