# Loop 135 Report -- 2026-06-04

## Summary

**Completed the root-cause fix of the canonical GoldenFloat16 master set (executes
the prior loop's Option A): fixed `t27/rtl_gen/gf16_add.v` (the master copied into
Gamma/Phi/Euler; byte-identical to Phi/Euler's shipped copy). It used a truncating
alignment with no guard/sticky bits -- up to ~512 ULP off on subtractive
cancellation / far-apart adds. Replaced it with the verified algorithm (= the
exhaustively-checked `gf16_v2_add`: 3 guard bits, sticky-capturing alignment,
priority renormalize, round-to-nearest ties-to-even, signed exponent) and
cross-checked it EXACT-equal to `gf16_v2_add` over 41286 representative +
cancellation + special pairs (0 diffs), verilator -Wall clean. Also fixed two
pre-existing crashes in `t27/rtl_gen/golden_tests.py` (missing module-level `import
sys`; `spec` unset on the FileNotFoundError path) so it runs again. With last loop's
gf16_mul fix, the canonical GF16 multiply AND add masters are now both correct -- so
any future tapeout or regeneration from t27 starts correct. The fabricated dies'
frozen `src/gf16_*.v` are left untouched (source must match the taped-out silicon).**

## Why

Last loop fixed the canonical gf16_mul; gf16_add was the other half of the same MAC
path (it contributes the ~0.11% RMS floor in the dot-product impact study). Fixing it
at the master completes the GF16 root-cause correction so the bug class cannot
re-propagate from the source.

## Implementation (in the t27 repo)

- Replaced `t27/rtl_gen/gf16_add.v` body with the verified `gf16_v2_add` (module kept
  `gf16_add`); documented the defect + fix in the header.
- Fixed `golden_tests.py`: added top-level `import sys`; set `spec = {}` on the
  spec-file-not-found path (it was referenced unbound in the phi_identity output).
- Committed to t27 (`58496433`, branch `trinity-rust-rings`, `Fixes #129` -- the GF16
  correctness issue; body notes scope = the adder, not the full NMSE benchmark).

## Verification

```
fixed canonical gf16_add == verified gf16_v2_add over 41286 pairs: 0 diffs
  (40000 representative near-1.0 + cancellation + special-value pairs)
verilator -Wall: gf16_add clean
golden_tests.py: runs (exit 0), regenerates golden_tests.json
t27 commit 58496433; fabricated dies' frozen src/gf16_add.v NOT modified
```

## Process notes (encountered, worth flagging)

- t27 enforces an L1-traceability `commit-msg` hook (must reference `Closes/Fixes/
  Resolves #N`); it was added AFTER last loop's gf16_mul commit (which landed without
  one). Referenced the real GF16-correctness issue #129.
- `gh` auth in this environment is broken (HTTP 401 Bad credentials), so issues could
  not be listed/created via CLI -- found #129 from in-repo docs instead.

## Research (this loop, web available)

This closes the root-cause arc cleanly: the same code-gen master fed one defect into
three dies for each of two operations (mul, add), so two master fixes (verified
against an independent exhaustive oracle, 0 diffs) immunize every future consumer.
The governance detour is itself a small lesson -- a traceability gate that requires a
closing verb forced a choice between "no issue ref" and "slightly over-scoped Fixes";
the honest resolution was to reference the genuinely-related issue and state the scope
in the body, since on a feature branch the verb does not auto-close. Fixing the two
latent `golden_tests.py` crashes (a verification script that could not run is a silent
gap) is the same "make the checks executable" discipline as wiring CI gates.

## Metrics

- Canonical GF16 master: gf16_mul (L134) + gf16_add (L135) both root-cause fixed
- gf16_add validation: 0 diffs vs verified gf16_v2_add over 41286 pairs; -Wall clean
- t27 golden_tests.py: 2 pre-existing crashes fixed -> runnable
- Frozen dies untouched; future tapeouts/regens now correct for both GF16 ops
- Days to deadline: 18

---

## Three Collaboration Options for the Next Loop

### Option A: Wind down -- root cause is fixed for both GF16 ops (strong rec.)
Both active silicon defects characterized + impact-measured + per-die fixed (staged +
CI-gated) + fixed at the canonical t27 master (mul L134, add L135); Corona lint-clean;
respin brief complete. There is no remaining defect to fix without a real workload or
a scheduled respin. **Recommendation: pause the cron (`CronDelete a10f282e`) and
resume near silicon (~Nov 2026).**

### Option B: Extend the master fix to the wider gfN family in t27 (if a generator exists)
The dies carry gf4..gf256 add/mul; the t27 master only hand-holds gf4/gf8/gf16. If the
wider rungs are generated, locate and fix that generator template (the same one-bit
width class) so the whole ladder regenerates correct -- the generator-level root cause.

### Option C: Real-workload task impact (if a trained model + data exist)
Replace the synthetic-classifier ~0.24% proxy with a true accuracy delta on the
MNIST/IGLA workload the dies were validated on (shipped vs gf16_v2).
