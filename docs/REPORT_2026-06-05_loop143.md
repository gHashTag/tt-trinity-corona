# Loop 143 Report -- 2026-06-05

## Summary

**Executed Option C: backported Gamma's verified gfN adders to Phi and Euler, closing
the last cross-die inconsistency. The loop-125 audit had shown Phi/Euler each carry 9
buggy gfN_add spec rungs (gf4..gf256) -- the same normalization/width/special-constant
bugs Gamma had pre-fix -- but they are OFF the compute path (standalone spec/library
RTL, not instantiated on either die's silicon top, not in the GDS), so they were left
documented rather than fixed. This loop replaced all 9 with Gamma's verified versions
(GRS rounding, signed exponent, correct special encodings; gf_add_sweep <= 0.667 ULP).
The wider gfN_mul spec units are absent in Phi/Euler (they only carry gf16_mul, the
compute one), so nothing to backport there. After the backport, all 9 units are
verilator -Wall clean on both dies and the cross-die audit reads 0 buggy rungs on all
three dies (was Gamma 0 / Phi 9 / Euler 9). Dead-code library fix -- no silicon
impact; the frozen on-die units (gf16_mul/add) are untouched.**

## Why

A defect documented as "off-path, left as-is" is still a latent trap for any future
reuse of those library units. Gamma's adders were already fixed and verified; making
Phi/Euler match is mechanical, low-risk (the units are not in the GDS), and turns the
cross-die audit from 0/9/9 into 0/0/0 -- a clean, measurable completion.

## Implementation

- Copied Gamma's verified `gf{4,8,12,20,24,32,64,128,256}_add.v` to `tt-trinity-phi`
  and `tt-trinity-euler` `src/`. (Note: zsh does not word-split unquoted vars -- used
  an explicit literal file list.)
- Verified each lints `-Wall` clean and re-ran `tools/cross_die_gf_audit.py`.
- Committed: phi 6f36161, euler 0442d46.

## Verification

```
9 backported adders (gf4..gf256) on phi + euler: all verilator -Wall clean
cross_die_gf_audit.py: gamma 0, phi 0, euler 0 buggy rungs (was 0/9/9)
gfN_mul spec units: absent in phi/euler (only gf16_mul present) -> none to backport
frozen on-die units (gf16_mul/add) untouched; off-path dead-code fix only
```

## Research (this loop, web available)

The close-out is the shared-source corollary once more: the same code-gen seeded the
gfN library into three dies, the fix lived in one, and consistency requires fanning
the fix back to all consumers -- even the dead-code copies, or the next reuse
inherits the bug. The audit metric (0/9/9 -> 0/0/0) is the honest scoreboard: it
states exactly what was inconsistent and confirms it no longer is. Distinguishing the
on-die units (frozen, untouched) from the off-path library units (safe to fix) is the
discipline that lets a "frozen silicon" repo still carry a correct library for the
next lineup without disturbing the taped-out netlist.

## Metrics

- gfN_add backported to 2 dies: 9 units each (gf4..gf256), all -Wall clean
- Cross-die audit: 0/9/9 -> 0/0/0 buggy rungs
- gfN_mul: absent in phi/euler -> no backport needed
- Frozen on-die units untouched; dead-code library fix
- Days to deadline: 17

---

## Three Collaboration Options for the Next Loop

### Option A: Wind down -- cross-die libraries now consistent + everything gated (strong rec.)
Every gfN unit is correct on all three dies (audit 0/0/0); both on-silicon compute
paths are functionally verified + CI-gated; the width class is gated everywhere; the
respin brief is complete. No further defect remains without external workload data.
**Recommendation: pause the cron (`CronDelete d3317e31`) and resume near silicon.**

### Option B: Provide a trained gf16 model + eval set (the one remaining real lever)
Drop in a small model that runs the gf16 path + a few hundred eval samples; I'll
report the true shipped-vs-gf16_v2 accuracy delta -- the definitive respin number.

### Option C: Functionally gate the gfN add library in Phi/Euler CI
Now that Phi/Euler's adders are correct, port `gf_add_sweep.py` (add-only rungs) into
their CI so the backported library stays functionally verified, not just lint-gated --
the same detect/fix/gate discipline applied to the now-consistent gfN library.
