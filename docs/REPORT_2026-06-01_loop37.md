# Loop 37 Report — 2026-06-01

## Summary

Comprehensive PLAN.md sync: updated all stale "77 formats" → "80 formats" and "TTGF26b" → "TTGF26a" references across the entire codebase. Fixed 10 active files. Historical loop reports left unchanged (they record what was true at the time).

## Changes

### 1. PLAN.md Full Sync (CRITICAL)

**File:** `PLAN.md`

- All ~30 "TTGF26b" references → "TTGF26a" (Corona's actual target shuttle)
- All ~20 "77 format/record" references → "80"
- Section 3.1 cluster table: MLLowPrecision 7→8, OCP MX 3→5, total 77→80
- Sizing: "770 bytes" → "800 bytes", "77-entry ROM" → "80-entry ROM"
- Index ranges: "0-76" → "0-79"
- TRI-NET lineup table: tile size "8x2 or 8x4 (Phase A)" → "4x4 (16 tiles)"
- Glossary: TTGF26a shuttle close date updated from "Q4 2026" to "2026-06-22"
- Phase E/B tables: record counts updated

### 2. README.md

Lines 22, 45: "77" → "80"

### 3. Spec and Source Files

| File | Change |
|------|--------|
| `specs/corona/corona_oracle.t27` line 48 | "all 77 numeric-format" → "all 80" |
| `specs/corona/corona_oracle.t27` line 165 | "77-record ROM" → "80-record ROM" |
| `tools/gen_rom.py` line 97 | "77 format records (0-76)" → "80 format records (0-79)" |
| `tools/README.md` lines 7, 15 | "77-record" → "80-record" |
| `src/rtl/README.md` line 9 | "77-record ROM" → "80-record ROM" |
| `src/tb/README.md` line 11 | "77-record sweep" → "80-record sweep" |
| `docs/adr/0001-tile-size-4x4.md` line 14 | "77 records" → "80 records" |

### 4. Research Findings

**IEEE P3109 minifloat standard** (Interim Report v3.0, Aug 2025): defines parametric `binaryKpP` from 3-bit to 16-bit. Subsumes FP8 E4M3, FP4, BF16 under unified naming. Lean formalization (FLoPS) published. No immediate action needed; our ROM already covers these formats.

**Takum hardware codec** (Hunhold arXiv:2408.10594v3): 38% latency reduction over posit. Upgrading takum status from [Experimental] to [Empirical fit] is defensible. No code change — ROM status_id is already correct.

**OCP MX+** (MICRO 2025) and **NVFP4** (NVIDIA Blackwell): New sub-byte formats not in our catalog. Candidate additions for a future catalog expansion beyond 80.

**GF180MCU gotchas**: 50 MHz max clock (matches our design), Metal 5 reserved (TT power grid), parasitic extraction for 11K top-metal variant may be inaccurate.

## Verification Status

```
Verilator lint:     PASS (0 warnings)
YAML parse:         PASS
Stale "77" scan:    PASS (0 remaining in active files)
```

## Metrics

- Files updated: 10 (PLAN.md, README.md, 2 specs, 2 READMEs, gen_rom.py, ADR, info.yaml, fv_rom.sv)
- Stale "77" in active files: ~50 → 0
- Stale "TTGF26b" in PLAN.md: ~30 → 0
- Commits: 47 total, 46 unpushed
- Days to deadline: 21

---

## Three Collaboration Options for Next Loop

### Option A: Push to GitHub (User Action Required)
Generate a GitHub PAT and run: `git remote set-url origin https://<TOKEN>@github.com/gHashTag/tt-trinity-corona.git && git push -u origin main`. This unblocks CI, GDS hardening, and submission. **The single highest-impact action — all CI, formal proofs, and GDS flow are blocked on this.**

### Option B: Sub-byte Decoder Exhaustive Golden LUTs
Add exhaustive golden LUTs to formal proofs for fp4 (16 entries), nf4 (16), bitnet (4), int4 (16). Currently these use algorithmic golden refs — exhaustive LUTs would eliminate formula errors. Medium effort, high confidence gain.

### Option C: Protocol v2 Formal Hardening
Add formal properties to fv_fsm.sv for the byte_count clamp edge cases: CMD2 with byte_count > 4, CMD2 with byte_count = 0 (ROM readback), rapid CMD1 re-entry. Currently covered by liveness bounds but not by explicit edge-case assertions.
