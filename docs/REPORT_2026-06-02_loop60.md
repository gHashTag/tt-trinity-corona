# Loop 60 Report -- 2026-06-02

## Summary

**ena=0 multi-state test added (DATA + STATUS freeze/resume). ROM spec cross-check promoted to CI job. 49 cocotb tests. Audit: 6 new areas — 1 HIGH (tt/ unpinned, mitigated by tt-gds-action), 2 MEDIUM (Timer(1,ns) fragility, config.json overlap), 3 LOW. GDS #26785592328: 9th consecutive precheck PASS. Research: 17 new findings including b-posit hardware efficiency parity, MicroMix multi-format GEMM, BitRL ternary RL.**

## Weakness Audit: 6 New Areas

### HIGH (1, mitigated)

| # | Finding | Status |
|---|---------|--------|
| H1 | `tt/` directory is NOT a git submodule — untracked, unpinned local clone of tt-support-tools. CI uses `tt-gds-action@ttgf26a` which provisions its own tools, so GDS flow works. `user_config.json` DEF template path resolves because tt-gds-action sets up tools before OpenLane runs. | **MITIGATED** — works by design of tt-gds-action; local `tt/` is diagnostic only |

### MEDIUM (2)

| # | Finding | Status |
|---|---------|--------|
| M1 | `Timer(1, units="ns")` in cocotb tests — reliable for Icarus RTL sim (40ns clock, 1ns is ample), but fragile if tests were reused for SDF-annotated gate-level cocotb sims. GLS smoke test uses self-contained Verilog TB, so GLS path is safe. | **ACCEPTED** — Icarus-only in CI; document if porting to GLS cocotb |
| M2 | `config.json` and `user_config.json` both contain `DIE_AREA` and `RT_MAX_LAYER` with identical values. `user_config.json` is gitignored (regenerated by tt_tool.py). No current conflict, but manual config.json edits could be silently overridden. | **ACCEPTED** — duplicated keys are effectively config.json defaults |

### LOW (3, all clean)

| Area | Finding |
|------|---------|
| Combinational loops / latches | All `always @(*)` blocks have default assignments. `lns8_decode.v` frac_lut case covers all 16 values of 4-bit input (no explicit default but functionally complete). Verilator `-Wall` passes clean. No latches inferred. |
| Formal cover trace utilization | All 8 cover points pass (7 VCD traces generated). Cover traces are diagnostic, not regression artifacts — standard sby workflow. All FSM states reachable within depth 30. |
| Anchor probe under ST_STATUS | Output mux at line 353: `ST_STATUS` takes priority over `is_anchor_cmd`. If FSM is in STATUS and `ui_in=0x7F`, output is `status_byte`, not anchor. Correct behavior. Formal P4 proves `fmt_id_r != 7'h7F` always. |

## Implementation

### 1. ROM Spec Cross-Check CI Job

Added `test_rom_spec_crosscheck.py` as a dedicated CI step in `.github/workflows/ci.yml`:
```yaml
- name: ROM spec cross-check (23 formats vs published specs)
  run: python3 test/test_rom_spec_crosscheck.py
```
This ensures any future ROM edits are validated against independently derived published spec values.

### 2. ena=0 Multi-State Test

Added `test_ena_gate_data_and_status` to `test/test_stress.py`:
- **Case 1**: ena=0 during ST_DATA — FSM frozen for 5 cycles, resumes and correctly consumes next byte, INT8 decode matches reference
- **Case 2**: ena=0 during ST_STATUS — first status byte read, freeze 5 cycles, remaining bytes read correctly, FP4 decode matches reference

This extends ena=0 coverage beyond the existing formal proof (P3, all states) and the Loop 55 cocotb test (ST_CMD2 only).

### 3. Test Count Updates

README.md and src/tb/README.md updated: 48 → 49 (5 anchor + 29 decoders + 15 stress).

## Scientific Research

### MX/OCP Format Hardware

| Paper | Source | Key Finding |
|-------|--------|-------------|
| MX Precision-Scalable PE | arXiv:2505.22404 (ISLPED 2025) | KU Leuven PE supporting all 6 MX data types via sub-word parallelism, 4x training throughput |
| MicroMix | arXiv:2508.02343 | Co-designed GEMM kernel mixing MXFP4/6/8 channels within single matmul on Blackwell |
| SFMP | arXiv:2602.01027 | Fractional bit-width quantization with unified GEMM kernel — future extension surface for Corona |
| MX PTQ Benchmark | arXiv:2601.09555 | Systematic study: MXFP8 near-lossless, MXFP4 scale factor quantization is dominant error source |

### Posit & Takum

| Paper | Source | Key Finding |
|-------|--------|-------------|
| b-posit | arXiv:2603.01615 | Bounded posit with regime cap at 6 bits — 79% less power, reaching IEEE float32 decoder cost parity |
| Adaptive Domain Models | arXiv:2603.18104 | Posit+quire accumulation viable for on-device AI training |
| Takum | IEEE Spectrum Feb 2026 | Hunhold at Openchip Barcelona profile; libtakum C99 + Takums.jl active development |

### BitNet / Ternary

| Paper | Source | Key Finding |
|-------|--------|-------------|
| BitRL | arXiv:2604.24273 | BitNet b1.58 ternary weights for RL agents: 10-16x memory reduction, 3-5x energy savings |
| Litespark | arXiv:2605.06485 | Multiplication-free ternary inference via integer dot-product SIMD, 52x throughput |

### Block Floating Point & FPGA

| Paper | Source | Key Finding |
|-------|--------|-------------|
| BFP Accumulation Boundaries | ScienceDirect FGCS vol.177 | FP accumulator dominates area at low BFP widths (77.1% for BFP2), Frobenius-norm precision framework |
| FPQVAR | arXiv:2505.16335 | FPGA VAR accelerator with LUT-based FP quantizer — similar pattern to Corona's ROM-based decode |

### Ecosystem

| Item | Source | Key Finding |
|------|--------|-------------|
| TTGF26a | tinytapeout.com | Shuttle open, closes Jun 22. Chips expected Oct 2026 |
| ChipFoundry.io | eenewseurope.com | Former eFabless founders, re-enables SKY130 access. TT plans SKY130 shuttle Jul 2026 |
| wafer.space Run 1 | Crowd Supply | 1000 GF180MCU bare dies shipping at $7/die since Mar 2026 |
| DAC 2026 | dac.com | Jul 26-29 Long Beach, 55% AI & Design coverage, paper titles not yet indexed |

## GDS Status

```
Precheck passes: 9 CONSECUTIVE (10th pending)
GDS #26785963978:  in progress (Loop 59 commit 60a8dc0)
  gds: PASS, gl_test: running, precheck: running
GDS #26785592328:  PASS (Loop 58 commit 454d459)
  gds: PASS, gl_test: PASS, precheck: PASS (viewer fails — cosmetic, known)
Previous: 8 consecutive passes confirmed
All with: BOTTOM_MARGIN_MULT=1, TOP_MARGIN_MULT=1
```

## Verification Status

```
Verilator lint:        PASS (0 warnings)
Yosys synthesis:       PASS (2,308 cells)
Formal (sby):          19 configs, 57 tasks (bmc+prove+cover)
Cocotb:                49 tests (was 48, +1 ena gate multi-state)
GL tests:              PASS (48/48 on Loop 59 commit, 49 pending)
GLS smoke:             PASS (76 golden vectors)
ROM spec cross-check:  PASS (23/23 formats, now in CI)
GitHub CI:             PASS (all green)
GDS + Precheck:        PASS (9 consecutive, 10th pending)
Submission status:     READY
```

## Audit Coverage (Loops 54-60)

| Loop | Areas | Key Findings |
|------|-------|-------------|
| 54 | Precheck root cause | MARGIN_MULT fix (CRITICAL, fixed) |
| 55 | Shift register, mux, docs | byte_count test, LibreLane rebrand |
| 56 | Multi-byte alignment, ROM golden, timing, pins, reset, unused outputs | All 8 clean |
| 57 | FSM transitions, ROM completeness, CDC, synthesis, test gaps, CI, tiles | TF32+BCD tests added |
| 58 | Cocotb compat, formal depth, GLS parity, gen_rom, D2D, Makefile, fmt_id=0, SDC | total_bits overflow documented |
| 59 | ena=0 all states, ROM latency, MODULE order, golden independence, PDK variant, source_files order, tb_rom independence | Circular golden dependency broken |
| 60 | Comb loops/latches, Timer(1,ns), formal covers, anchor under STATUS, tt/ submodule, config overlap | tt/ unpinned (mitigated), Timer fragility noted |

**Total: 29+ audit areas across 7 loops. 0 CRITICAL, 0 HIGH blocking, 0 open issues. 1 open user decision (discord field).**

## Metrics

- New audit areas investigated: 6
- Issues found: 1 HIGH (tt/ unpinned, mitigated), 2 MEDIUM (accepted), 3 LOW
- New test: ena=0 DATA+STATUS freeze/resume
- CI improvement: ROM spec cross-check now a CI gate
- Cocotb tests: 49 (was 48)
- New papers cataloged: 12 papers + 5 ecosystem updates
- Days to deadline: 20

---

## Three Collaboration Options for Next Loop

### Option A: Final Submission — Fill Discord + Freeze + Post-Silicon Prep

All critical audit findings resolved across 7 loops. 29+ areas investigated, 0 blocking issues. Fill discord in info.yaml, submit to TTGF26a, and freeze the codebase. Begin post-silicon preparation: port cocotb anchor probe and ROM readback tests to microcotb for RP2350 demoboard HIL testing. Prepare format coverage table for ARITH 2026 (June 28, Fulda) community engagement.

### Option B: Timer Robustness + lns8 Default Branch + 50th Test

Three targeted improvements: (1) Replace `Timer(1, units="ns")` with `await FallingEdge(dut.clk)` in `read_result_bytes()` for simulator-portable sampling. (2) Add explicit `default` to `lns8_decode.v` frac_lut case statement (functionally redundant but eliminates potential tool warnings). (3) Add 50th test — e.g., `test_all_decoder_round_trip` that exercises every decoder module with a single canonical value, verifying no decoder was accidentally broken during refactoring.

### Option C: b-Posit Format Analysis + MicroMix Multi-Format ROM Metadata

Deep-dive the b-posit (arXiv:2603.01615) bounded regime optimization — analyze whether Corona's posit8_decode.v already handles the regime cap or needs a b-posit mode. Simultaneously, analyze MicroMix's per-channel MX format mixing to determine if Corona's ROM needs a "mixable" flag or format-group metadata. Both feed into potential ROM record extensions for a future Corona v2.
