# Loop 35 Report — 2026-06-01

## Summary

Strengthened ROM formal verification from 4 to 28 properties (11 new golden spot-checks covering all 13 clusters, 10 field-range assertions, 13 cross-field consistency rules). Added GLS CI job. Fixed stale "77 records" → "80 records" across 4 spec files. Researched push alternatives and submission checklist.

## Changes

### 1. ROM Formal Verification Overhaul

**File:** `formal/fv_rom.sv`

Previous: 4 properties (self-index, default-zero, non-zero, 3 golden spot-checks).

New: 28 properties organized in 3 groups:

**P5: 11 new golden spot-checks** — one per previously uncovered cluster:
| Cluster | Addr | Description |
|---------|------|-------------|
| 1 (bfloat) | 6 | BF16 variant |
| 2 (mini-float) | 12 | FP6/FP8 |
| 3 (posit sweep) | 23 | Posit32 |
| 4 (integer/fixed) | 35 | INT32 |
| 6 (log-number) | 44 | LNS16 |
| 7 (fixed-point) | 50 | Block format |
| 8 (decimal-float) | 59 | Decimal64 |
| 9 (quantized) | 66 | Stochastic |
| 10 (legacy) | 70 | Oddball |
| 11 (extended) | 73 | FP128 |
| 12 (custom) | 76 | Ultra-narrow |

**P6: 10 field-range assertions** — every ROM field bounded:
- cluster_id ≤ 12, status_id ∈ {1,2,5,6,7}, total_bits even and ≤ 128
- sign_bits ≤ 1, exp_bits ≤ 88, mant_bits ≤ 236
- encoding_kind ≤ 9, phi_distance ≥ 0x01C8, ref_index ≤ 45
- flags ∈ {0,1,6,7,8}

**P7: 13 cross-field consistency rules** (R1-R13):
- Fixed-point (enc=3): exp_bits=0, sign+mant=total
- Decimal-float (enc=8): sign=1, s+e+m=total, status_id=6
- Log-number (enc=2): sign=1, s+e+m=total
- Posit (enc=7): sign=1
- Cluster 3: always enc=7, flags=6, ref_index=15
- Cluster 4: enc∈{1,6}, status_id=5
- Unsigned integer (enc=6): sign=1, exp=0, mant=0
- Integer sign-magnitude (enc=1): mant=0
- ref_index ≤ 79 (valid record pointer)

### 2. GLS CI Job

**File:** `.github/workflows/ci.yml`

New `gls` job that:
1. Installs Yosys + Icarus Verilog
2. Synthesizes to generic cells with `stat` output
3. Runs `tb_gls_smoke.v` against synthesized netlist
4. Fails if "ALL PASS" not found in output

### 3. Spec Consistency (77 → 80)

Updated 4 spec files to reflect the actual 80-record ROM:

| File | Changes |
|------|---------|
| `specs/corona/rom_layout.t27` | RECORD_COUNT 77→80, ROM_TOTAL_BITS 6160→6400, index range 0..76→0..79 |
| `specs/corona/corona_oracle.t27` | TOTAL_FORMATS 77→80, ml_low_precision 7→8, ocp_mx 3→5, ROM_RECORDS comment |
| `specs/corona/protocol.t27` | "77-record" → "80-record" |
| `specs/corona/d2d_routing.t27` | "77 SSOT format indices" → "80" |

### 4. Submission Research Findings

**Push alternatives** (since `gh auth login` is blocked):
```bash
git remote set-url origin https://<TOKEN>@github.com/gHashTag/tt-trinity-corona.git
git push -u origin main
```
Generate a fine-grained PAT at GitHub Settings → Developer Settings → Personal Access Tokens.

**Tile size "4x4"**: May not be a standard TT configuration. Standard sizes are 1x1, 1x2, 2x2, 8x2, 8x4. Needs verification against ttgf-verilog-template. The GDS action will validate it.

**Area estimate**: 2308 generic cells → ~2,800-5,800 GF180MCU cells after tech mapping → fits in 16 tiles (whether 4x4 or 8x2).

## Verification Status

```
Verilator lint:      PASS (0 warnings)
Yosys synthesis:     PASS (2308 cells)
Iverilog compile:    PASS (RTL + GLS)
GLS smoke test:      PASS (anchor + BF16)
ROM formal parse:    PASS (28 properties)
Formal (sby):        Pending CI
Cocotb tests:        Pending CI
```

## Metrics

- ROM formal properties: 4 → 28 (+600%)
- Golden cluster coverage: 3/13 → 13/13 (100%)
- CI jobs: 5 → 6 (+GLS)
- Stale "77" refs fixed: 4 files
- Commits: 44 total, 43 unpushed
- Days to deadline: 21

---

## Three Collaboration Options for Next Loop

### Option A: Push to GitHub (User Action Required)
Generate a GitHub PAT and run: `git remote set-url origin https://<TOKEN>@github.com/gHashTag/tt-trinity-corona.git && git push -u origin main`. This unblocks GDS hardening, CI, and submission. The single highest-impact action — everything else is blocked on this.

### Option B: PLAN.md Spec Sync (77 → 80)
PLAN.md has 10+ references to "77 formats" that are now stale. Update all references, cluster breakdowns, and sizing estimates to match the 80-record reality. Also update the shuttle reference from "TTGF26b" to "TTGF26a" and tile size discussion.

### Option C: Exhaustive ROM Golden in Formal
Replace the 14 spot-checks in fv_rom.sv with a full 80-entry golden LUT (80 × 80-bit constants). This would make the ROM proof as strong as the decoder proofs — every bit of every record verified. Effort: medium (need to extract all 80 constants from format_rom.v).
