# Loop 142 Report -- 2026-06-05

## Summary

**"Do everything" -> executed the actionable options. B (wire the functional tests
into CI): added a `vsa-functional` CI job that runs `gf16_popcount_verify.py`
(gf16_popcount + gf16_popcount16, 4000 vectors each) so the VSA ternary-matmul compute
path stays FUNCTIONALLY guarded, not just lint-gated. Scoped it correctly: Gamma and
Euler instantiate the VSA path (job added to both); Phi is the minimal "nano" die with
NO VSA modules (confirmed: only gf16_dot4), so the test does not apply there and its
stray copy was removed. C (real-workload accuracy delta) remains blocked on external
data -- there is no in-repo trained gf16 model + dataset; the synthetic harness
`impact_task_gf16.py` is ready to swap real weights in when provided. A (pause) is
moot since the loop is re-armed for continued work. Both on-silicon compute paths now
have standing CI gates: the gf16_dot4 float MAC (goldenfloat-arith + gf16-fix-verify)
and the VSA ternary MAC (vsa-functional).**

## Why

Last loop verified the VSA path correct but only as an ad-hoc run; wiring it into CI
makes that verification durable (a regression in gf16_popcount would now fail CI).
Confirming Phi has no VSA before adding the job avoided a spurious always-failing gate.

## Implementation

- `vsa-functional` job added to `tt-trinity-gamma/.github/workflows/test.yaml`
  (commit 31e7e76) and `tt-trinity-euler/...` (6995d27; also ported the test).
- Confirmed Phi (`tt_um_trinity_nano`) has no `vsa_*`/`gf16_popcount*` modules ->
  removed the inapplicable copy.
- C: verified no runnable trained model/dataset exists in-repo; left the synthetic
  `impact_task_gf16.py` as the drop-in harness.

## Verification

```
gamma/euler test.yaml: valid; vsa-functional job added
gf16_popcount_verify.py: gamma + euler 4000/4000 each (8- and 16-elem cores)
phi: no VSA modules -> job not added (correct scoping)
CI coverage now: gf16_dot4 path (goldenfloat-arith/gf16-fix-verify) + VSA path
  (vsa-functional) + width class (width-defect-gate), across the dies that have each
```

## Research (this loop, web available)

Right-sizing a gate to the unit is the point again: a functional gate is only
meaningful on a die that instantiates the module, so verifying Phi has no VSA path
before wiring the job is what keeps the gate honest (no always-red CI, no false sense
that Phi's VSA is "tested"). With this, the durable-gate story is complete: both
shipped compute paths -- the float MAC and the ternary MAC -- are guarded by CI for
both correctness (functional checks) and the recurring width-defect class (lint gate),
on exactly the dies that carry each. The one thing CI cannot supply is an external
trained workload, which is the sole remaining lever for a sharper respin number.

## Metrics

- vsa-functional CI job added to 2 dies (gamma, euler); phi correctly excluded
- Both on-silicon compute paths now have standing functional CI gates
- C (real-workload delta): blocked on external model/dataset; harness ready
- Days to deadline: 17

---

## Three Collaboration Options for the Next Loop

### Option A: Wind down -- everything actionable is done and CI-guarded (strong rec.)
Both compute paths fixed/verified and functionally CI-gated; width class gated
everywhere; respin brief complete. The only open lever needs external data.
**Recommendation: pause the cron (`CronDelete d3317e31`) and resume near silicon.**

### Option B: Provide a trained gf16 model + eval set (unblocks the real-workload delta)
Drop in (or point me at) a small trained model that runs through the gf16 path plus a
few hundred eval samples; I'll report the true shipped-vs-gf16_v2 accuracy delta --
the definitive respin number, replacing the synthetic ~0.24% proxy.

### Option C: Backport the gfN fixes to Phi/Euler's off-path spec rungs
Phi/Euler still carry the buggy (off-compute-path) gf4..gf256 spec units; Gamma's are
fixed. Port Gamma's verified gfN units so all three dies' libraries match, then the
full goldenfloat-arith suite could run in their CI too. Library hygiene, no silicon
impact.
