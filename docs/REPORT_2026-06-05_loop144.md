# Loop 144 Report -- 2026-06-05

## Summary

**"Do everything" -> executed the actionable option (C): functionally gated the
backported gfN adder library in Phi and Euler CI. Last loop backported Gamma's
verified gf4..gf256 adders to Phi/Euler (cross-die audit 0/0/0); this loop ports
`gf_add_sweep.py` and adds a `gfn-add-sweep` CI job to both, so the adders stay
functionally verified (gf8..gf256 vs an exact-rational reference, <= 1 ULP) rather
than only lint-gated. Confirmed both pass on the backported units (all rungs <= 0.667
ULP). Gamma already runs the same sweep inside its goldenfloat-arith job, so all three
dies now functionally CI-gate their gfN add library. B (real-workload accuracy delta)
remains blocked on external data; A (pause) is moot under a re-armed loop.**

## Why

A backported library that nothing re-checks drifts; a functional CI gate makes the
0/0/0 cross-die consistency durable. The adders are off the compute path, but gating
them keeps the library trustworthy for future reuse -- the detect/fix/gate discipline
applied to the now-consistent gfN adders.

## Implementation

- Ported `gf_add_sweep.py` to `tt-trinity-phi/test` and `tt-trinity-euler/test`; both
  PASS on the backported adders (gf8..gf256, <= 0.667 ULP).
- Added a `gfn-add-sweep` CI job to each `test.yaml` (phi ae66441, euler f191e9b).
- Phi has no VSA path, so it has no vsa-functional job (correct); it now runs
  gf16-fix-verify + width-defect-gate + gfn-add-sweep.

## Verification

```
gf_add_sweep.py on phi + euler: gf8..gf256 ALL PASS (<= 0.667 ULP) on backported adders
test.yaml (phi, euler): valid; gfn-add-sweep job added
gating now: gfN add library functionally gated on all 3 dies (gamma via goldenfloat-arith)
```

## CI gate coverage (end state)

| die | gf16 MAC (dot4) | VSA matmul | gfN add lib | width class |
| --- | --- | --- | --- | --- |
| Gamma | goldenfloat-arith | vsa-functional | goldenfloat-arith | width-defect-gate |
| Euler | gf16-fix-verify | vsa-functional | gfn-add-sweep | width-defect-gate |
| Phi   | gf16-fix-verify | (no VSA) | gfn-add-sweep | width-defect-gate |
| Corona | n/a (decoders) | n/a | n/a | full `-Wall` lint job |

## Research (this loop, web available)

The arc's recurring close-out: every fix this project made is now paired with a CI
gate sized to the unit that carries it -- functional gates on the compute paths and
the now-consistent adder library, a lint gate on the width-defect class, and a full
zero-warning lint on the clean oracle. The matrix above is the durable artifact: it
states, per die, which subsystem is guarded by which check, so a reviewer can see
coverage at a glance and a regression in any guarded path fails CI rather than
surfacing in a later silicon audit. The one thing still outside CI's reach is a real
trained workload, which is the sole remaining lever for sharpening the respin number.

## Metrics

- gfn-add-sweep CI job added to phi + euler; gamma already gated via goldenfloat-arith
- gfN add library now functionally CI-gated on all 3 dies
- B (real-workload delta): still blocked on external model/dataset
- Days to deadline: 17

---

## Three Collaboration Options for the Next Loop

### Option A: Wind down -- every fix is now paired with a CI gate (strong rec.)
Both compute paths verified + gated, the gfN library consistent (0/0/0) + functionally
gated on all dies, the width class gated everywhere, the respin brief complete. The
CI-gate coverage matrix is closed. No further defect remains without external workload
data. **Recommendation: pause the cron (`CronDelete d3317e31`) and resume near silicon.**

### Option B: Provide a trained gf16 model + eval set (the one remaining real lever)
Drop in a small model that runs the gf16 path + a few hundred eval samples; I'll
report the true shipped-vs-gf16_v2 accuracy delta -- the definitive respin number,
replacing the synthetic ~0.24% proxy.

### Option C: Implement a correct Posit16 codec (last known wrong implementation)
`gf16_to_posit16` / `posit16_to_gf16` are simplified stubs with an invalid
variable-length regime. Implement a spec-correct Posit16 (es=2) encoder/decoder with a
round-trip test -- the last documented wrong implementation in the GF library (dead
code today, but a genuine correctness fix for any future posit interchange path).
