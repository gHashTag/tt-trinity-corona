# Loop 15 Report — 2026-06-01

## Summary

Added 7 back-to-back protocol stress tests verifying the FSM handles
transactions without reset. Confirmed `data_in_buf` zeroing on CMD1 entry
prevents stale data corruption. 30/30 tests pass across all test suites.

## Implemented

### Protocol Stress Tests (`test/test_stress.py`)

| Test | What it verifies |
|------|-----------------|
| `test_back_to_back_same_format` | 16 consecutive FP4 decodes, no reset |
| `test_back_to_back_mixed_formats` | 10 different formats interleaved |
| `test_rom_then_decode` | ROM readback → immediate decode |
| `test_decode_then_rom` | Decode → immediate ROM readback |
| `test_multi_byte_then_single_byte` | BF16 (2-byte) → FP4 (1-byte) → INT8 |
| `test_3byte_tf32_then_1byte` | TF32 (3-byte) → E8M0 (1-byte) |
| `test_rapid_format_cycling` | 10 formats cycled without reset |

### FSM Audit Results

Audited the IDLE→CMD2→DATA→STATUS→DONE→IDLE cycle for back-to-back safety:

1. `data_in_buf` — cleared to 0 at IDLE→CMD2 (line 87). Safe.
2. `status_cnt` — reset to 0 at IDLE (line 83) and at DATA→STATUS (line 108). Safe.
3. `rom_mode` — cleared at IDLE (line 84) and CMD2 (line 98). Safe.
4. `data_cnt` — unconditionally set at CMD2 (line 93). Safe.
5. `fmt_id_r` — set at IDLE→CMD2 (line 86). Safe.

No register leakage found. The DONE state serves as a mandatory 1-cycle
gap between transactions (intentional by design).

## Research: FSM Verification Best Practices

| Technique | Applicability |
|-----------|--------------|
| State transition coverage | All 5 happy-path transitions hit |
| Constrained-random testing | cocotb-coverage library available |
| Back-to-back stress | Implemented (this loop) |
| Post-synthesis gate-level sim | Future work (needs GDS first) |
| Formal verification | Overkill for this FSM complexity |

## Metrics

- **Tests**: 30 (23 decoder + 4 anchor + 3 infrastructure → 7 stress)
- **Verified values**: 67,820+ (exhaustive decoders + 56 stress transactions)
- **FSM transitions tested**: IDLE→CMD2→DATA→STATUS→DONE→IDLE (all covered)
- **Commits**: 15 on main (14 ahead of origin)
- **Deadline**: 2026-06-22 (21 days remaining)

## Three Collaboration Options for Next Loop

### Option A: Push to GitHub + Trigger CI Pipeline
Run `gh auth login`, push all 14 commits, trigger both test CI and GDS
hardening. This is the single most impactful action remaining. The GDS
workflow will reveal synthesis/DRC issues immediately. Every loop without
pushing increases the risk of late-discovery blockers.

### Option B: Gate-Level Simulation Prep
Create the infrastructure for post-synthesis simulation. Generate a
gate-level netlist via Yosys using GF180MCU cells (if Liberty file
available), then run cocotb tests against it. This validates that
synthesis doesn't break functionality.

### Option C: Add Remaining Sub-8-bit Decoders
Add FP8 E4M3 FNUZ (no-NaN variant, fmt_id=69), Bitnet 1.58b ternary
(fmt_id=71), and NF4 bitsandbytes variant (fmt_id=75). These would
bring the on-die decoder count from 14 to 17, covering all FLAG_ON_DIE
formats in the ROM catalog.
