# Loop 140 Report -- 2026-06-04

## Summary

**Completed the "gate everywhere" close-out (executes the prior loop's Option B):
the recurring "a field one bit too narrow" defect class is now guarded by CI at every
level it can be. (1) Added a width-defect gate to the t27 RTL MASTER -- the
highest-leverage spot, since future tapeouts/regens copy from it: `rtl_gen/
width_gate.py` + a step in the `tri-test` format-validation job that verilator-lints
every `rtl_gen/*.v` and fails on a value-overflow or out-of-range select (passes
today -- the master is clean after the gf16_mul/gf16_add/gf_formats/gf4_add fixes;
t27 commit d11a1876, `Fixes #129`). (2) Found that CORONA already has a STRICTER gate
-- its `ci.yml` `verilator-lint` job runs `verilator --lint-only -Wall src/rtl/*.v`
with a zero-warning expectation, which I confirmed green (0 warnings) and which covers
the width class plus every other -Wall class; so no width-specific gate is needed
there. With the per-die gates added last loop (Gamma/Phi/Euler, allow-listing the
frozen gf16_mul), the class is now gated at the source master, the primary oracle
die, and all three compute dies.**

## Why

A swept-clean class needs a gate at the SOURCE (where new RTL is authored), not only
at the leaves (the dies). The t27 master is that source. Checking Corona before
adding a redundant gate found it already enforces a stricter full -Wall zero-warning
lint -- so the right action there was to verify it, not duplicate it.

## Implementation / findings

- t27 `rtl_gen/width_gate.py`: verilator-lints `rtl_gen/*.v`, fails on value-overflow
  / SELRANGE. Wired into `tri-test.yml` (format-validation job). PASS locally.
- Corona: `ci.yml` already has a `verilator-lint` job (`-Wall`, zero warnings) over
  `src/rtl/*.v`; confirmed it emits 0 warnings -> already covers the class (and more).
  No change made (a width-specific gate would be strictly weaker).

## Verification

```
t27 rtl_gen/width_gate.py: PASS (master clean); wired into tri-test (commit d11a1876)
corona verilator --lint-only -Wall -Isrc/rtl src/rtl/*.v: 0 warnings (existing gate green)
gating coverage now: t27 master (width gate) + Corona (full -Wall) + Gamma/Phi/Euler
  (width gate w/ frozen-gf16_mul allow-list, loop 139)
```

## Research (this loop, web available)

The close-out makes the ratchet complete: the cleaned invariant ("no field-too-narrow
defect") is now enforced where code enters -- at the authoring master, not only at the
fabricated leaves. The Corona observation is the useful negative: before adding a
guard, check whether a stronger one already exists; a full `-Wall` zero-warning lint
strictly dominates a single-class gate, so duplicating it would add maintenance with
no coverage. Matching the gate strength to the unit's constraints is the nuance -- the
dies can only run a targeted gate with an allow-list (they carry the frozen,
documented gf16_mul SELRANGE), while a clean unit like Corona can hold the line at
zero total warnings.

## Metrics

- t27 master: width-defect gate added (CI) -- source now guarded
- Corona: confirmed already gated by a stricter full -Wall zero-warning job (no change)
- Coverage: source master + 4 dies all CI-guard the "field too narrow" class
- t27 commit d11a1876; no frozen silicon touched
- Days to deadline: 18

---

## Three Collaboration Options for the Next Loop

### Option A: Wind down -- detect/fix/gate is complete everywhere (strong rec.)
The arc is closed end to end and now CI-guarded at every level: defects found,
impact-measured (MAC ~11x RMS, task ~0.24%), fixed per-die and at the t27 master,
proven Verilog-only, swept clean project-wide, and gated against regression on the
master + all four dies. Nothing further is fixable without a scheduled respin or a
real workload. **Recommendation: pause the cron (`CronDelete a10f282e`) and resume
near silicon (~Nov 2026).**

### Option B: Real-workload task impact (if a trained model + data exist)
Replace the synthetic-classifier ~0.24% proxy with a true accuracy delta on the
MNIST/IGLA workload (shipped vs gf16_v2) -- the one remaining number a respin
decision would sharpen. Needs the model + dataset.

### Option C: Align the spec<->RTL rounding convention (last cosmetic residual)
Spec round-half-up vs RTL ties-to-zero (<=1 ULP, 0.64% of products). Pick one
(ties-to-even) and make both match, verified by `spec_vs_rtl_gf16.py`. Low-risk,
cosmetic; only for a bit-exact conformance sign-off.
